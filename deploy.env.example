# คัดลอกเป็น deploy.env แล้วใส่ URL repo จริง
# cp deploy.env.example deploy.env

# HTTPS หรือ SSH
GIT_REMOTE_URL=https://github.com/YOUR_USER/trading-avelqua.git
GIT_BRANCH=main

# หลัง pull แล้ว restart อัตโนมัติ
PM2_APP_NAME=trading-avelqua
RUN_NPM_INSTALL=1
